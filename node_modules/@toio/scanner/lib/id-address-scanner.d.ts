import { Peripheral } from 'noble-mac';
import { Scanner } from './scanner';
export declare class IdAddressScanner extends Scanner {
    private readonly targets;
    private readonly fuzzy;
    constructor(targets: string[], fuzzy?: boolean, timeoutMs?: number);
    protected onDiscover(peripheral: Peripheral): void;
    protected executor(): void;
    private match;
}
