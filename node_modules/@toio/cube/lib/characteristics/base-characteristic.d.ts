/// <reference types="node" />
import { EventEmitter } from 'events';
import { Characteristic } from 'noble-mac';
export declare abstract class BaseCharacteristic<Event> {
    private readonly characteristic;
    private readonly eventEmitter;
    constructor(characteristic: Characteristic, eventEmitter: EventEmitter);
    protected abstract onData(data: Buffer): void;
    private shouldSubscribe;
}
